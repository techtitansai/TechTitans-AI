{
  "name": "techtitans-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd artifacts/techtitans-ai && npm install && npx vite build --config vite.config.ts && mkdir -p ../../dist && cp -r dist/public/* ../../dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "nodemailer": "^6.10.0"
  }
}
